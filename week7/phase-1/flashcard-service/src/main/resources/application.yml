server:
  port: 8081
spring:
  application:
    name: flashcard-service
  datasource:
    password: password
    url: jdbc:h2:mem:memdb
    username: sa
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
